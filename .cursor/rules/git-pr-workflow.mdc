---
description: 禁止直推 main；开发分支 + 自测 + PR 合入；合入后清理；完成前验证
alwaysApply: true
---

# Git / PR 工作流（必须遵守）

本仓库 **禁止** 在 `main` 上直接开发、提交或推送功能改动。所有代码变更走开发分支 + Pull Request。

## 标准步骤

1. 同步 `main`，再开分支（勿在 `main` 上改代码）：
   ```bash
   git checkout main && git pull origin main
   git checkout -b <type>/<short-name>
   ```
2. 只在该分支上修改、提交。**推送 / 开 PR / 声称完成前**必须有新鲜门禁证据：
   ```bash
   make lint && make typecheck && make test
   ```
   （改动极小时至少跑相关 pytest；业务代码被 Debug 改过则必须再跑测试。）
3. `git push -u origin HEAD`，以 `main` 为 base 开 PR（用仓库 PR 模板）。
4. 合入后立刻：
   ```bash
   git checkout main && git pull origin main
   git branch -d <type>/<short-name>          # 本地
   git push origin --delete <type>/<short-name>  # 远程（若仍存在）
   ```
5. Agent 被要求「提交 / 开 PR」时：若当前在 `main`，**先建分支再提交**；**绝不** `git push origin main`。

## 分支命名

| 前缀 | 用途 |
| --- | --- |
| `feat/` | 新功能 |
| `fix/` | Bug 修复 |
| `docs/` | 文档 / 注释 |
| `chore/` | 工具链、流程、配置 |

## Autopilot / CI 阻挡合入时

- 优先修与本 PR **相关** 的失败。
- 若失败在 `main` 已存在、且为**最小安全修复**（如单文件 `ruff format`）并挡死本 PR：允许在本 PR 分支做该最小修复并注明「unblocking CI」，或先单独 `fix/` PR 修主干再 rebase。
- **禁止**为过 CI 而改 `.github/workflows` 放宽检查。

## 禁止事项

- 在 `main` 上 `git commit` 后直接推远程（含 force push）
- 跳过自测就开 PR / 声称完成
- 合入后长期保留已合并功能分支（易漏提交）
- 用 amend / force push 改写已推送的共享分支（除非用户明确要求）

## 合并建议

- 默认 Squash merge 进 `main`
- PR 描述写清 Summary + Test plan；涉及规格时勾选验收项

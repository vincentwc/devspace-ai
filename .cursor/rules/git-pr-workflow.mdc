---
description: 禁止直推 main；开发分支 + 自测 + PR 合入的标准流程
alwaysApply: true
---

# Git / PR 工作流（必须遵守）

本仓库 **禁止** 在 `main` 上直接开发、提交或推送功能改动。所有代码变更走开发分支 + Pull Request。

## 标准步骤

1. 同步 `main`，再开分支（勿在 `main` 上改代码）：
   ```bash
   git checkout main && git pull origin main
   git checkout -b <type>/<short-name>
   ```
2. 只在该分支上修改、提交；本地自测通过后再推远程：
   ```bash
   # 至少：相关测试；有条件时 make lint && make typecheck && make test
   git push -u origin HEAD
   ```
3. 以 `main` 为 base 创建 PR；合并后再切回 `main` 并 `git pull`。
4. Agent 被要求「提交 / 开 PR」时：若当前在 `main`，**先建分支再提交**；**绝不** `git push origin main`。

## 分支命名

| 前缀 | 用途 |
| --- | --- |
| `feat/` | 新功能 |
| `fix/` | Bug 修复 |
| `docs/` | 文档 / 注释 |
| `chore/` | 工具链、流程、配置 |

示例：`feat/case-draft-export`、`fix/model-timeout-mapping`、`chore/dev-pr-workflow`。

## 禁止事项

- 在 `main` 上 `git commit` 后直接推远程（含 force push）
- 跳过自测就开 PR / 声称完成
- 用 amend / force push 改写已推送的共享分支（除非用户明确要求）

## 合并建议

- 默认 Squash merge 进 `main`（保持主分支历史清晰）
- PR 描述写清 Summary + Test plan
